rest:
  # Endpoint to receive messages from other services.
  # This is not needed for your backend proxy, but is useful for local testing.
  # Example: curl -X POST http://localhost:5005/webhooks/rest/webhook -d '{"sender":"user","message":"bonjour"}'
  #         -H "Content-Type: application/json"

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true
